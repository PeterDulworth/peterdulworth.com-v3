import React from 'react';
import ThemeToggle from './ThemeToggle';

const Nav = () => {
    return (
        <nav className="Nav">
            <ThemeToggle />
        </nav>
    );
};

export default Nav;